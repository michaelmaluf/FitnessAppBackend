// import { RequestHandler } from 'express';
// import { loginUserValidation } from '../validator';
// import { userSchema } from './userSchema';

// export const signupUserValidation: RequestHandler = (req, res, next) =>
//   loginUserValidation(userSchema.signupUser, req.body, next);

// export const loginUserValidation: RequestHandler = (req, res, next) =>
//   validator(userSchema.loginUser, req.body, next);
